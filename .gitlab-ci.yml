stages:
  - test
  - prod

test:
  stage: test
  script:
  - pip install --no-cache-dir -r requirements.txt
  - pylint pybus.py
  - python pybus.py test

production:
  stage: prod
  script:
  - pip install --no-cache-dir -r requirements.txt
  - python pybus.py prod
  only:
  - master
